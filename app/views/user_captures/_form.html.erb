<% if @industries %>

<%= form_for(user_capture) do |f| %>
    <p><strong>Enter your email and industry to register for early access.
    We’re not quite ready to go live yet, but by signing up you automatically qualify for launch status and offers.</strong><p>

  <div class="field">
    <%= f.label :email %>
    <%= f.email_field :email, placeholder: 'Your email' %>
  </div>
  <div class="field">
    <%= f.label :industry %>
    <div class="select">
      <%= f.select :industry, @industries %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit 'Register for early access', class: 'btn btn-primary button' %>
  </div>
<% end %>

<% end %>
